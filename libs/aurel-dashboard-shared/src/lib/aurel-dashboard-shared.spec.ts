import { aurelDashboardShared } from './aurel-dashboard-shared';

describe('aurelDashboardShared', () => {
  it('should work', () => {
    expect(aurelDashboardShared()).toEqual('aurel-dashboard-shared');
  });
});
