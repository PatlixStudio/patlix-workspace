# aurel-dashboard-shared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build aurel-dashboard-shared` to build the library.

## Running unit tests

Run `nx test aurel-dashboard-shared` to execute the unit tests via [Jest](https://jestjs.io).
